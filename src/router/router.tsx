import { Route, Routes } from "react-router-dom";
import { ScrollTop } from "../ScrollTop";

const GlobalRouter = () => {
  return (
    <ScrollTop>
      <Routes></Routes>
    </ScrollTop>
  );
};

export default GlobalRouter;
